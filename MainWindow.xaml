<Window x:Class="Project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project"
        xmlns:UserControls="clr-namespace:Project.Views.UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="1200">
    <Window.Resources>
        <Style TargetType="{x:Type UserControls:TimeGrid}">
            <Setter Property="Margin" Value="20,0"/>
        </Style>
        <Style TargetType="{x:Type UserControls:TimeAxis}">
            <Setter Property="Margin" Value="20,0"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <!-- Przycisk i błędy -->
            <RowDefinition Height="60"/>
            <!-- Oś -->
            <RowDefinition Height="*"/>
            <!-- Zawartość -->
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <!-- Przycisk i błędy -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" BorderBrush="Black" BorderThickness="1.4,1.4,1.4,1.4" Width="120" Height="60" Cursor="Hand">
                <Button x:Name="FileLoad" Content="Wczytaj z pliku" Click="FileLoad_Click" VerticalAlignment="Stretch" Background="Transparent" BorderThickness="0"/>
            </Border>
            <TextBlock Grid.Column="1" x:Name="Errors" TextWrapping="Wrap" Text="Potencjalne błędy pojawią się tutaj" FontSize="12" Height="60" TextAlignment="Center" Foreground="#FFFF3131"/>
        </Grid>
        <Grid Grid.Row="1">
            <!-- Oś -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <UserControls:TeamLabel Grid.Column="0" TeamName="Team" Height="60" Width="120"/>
            <UserControls:TimeAxis x:Name="TimeAxis"  Grid.Column="1" Height="60" Width="Auto"/>
        </Grid>
        <!-- Scroll po teamach (pionowy) -->
        <ScrollViewer x:Name="TeamDataScrollViewer" 
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto"
            CanContentScroll="False"
            Height="Auto"
            Grid.Column="0"
            Grid.Row="2"
            Background="Transparent"
            >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition x:Name="ContentColumn" Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel x:Name="TeamLabels" Grid.Column="0" Orientation="Vertical">
                </StackPanel>
                <StackPanel x:Name="TeamGantt" Grid.Column="1">
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
